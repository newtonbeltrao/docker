### "olá mundo" em Golang no Docker ###

# Gerar a Build: [sudo] docker build -t go-hello-world-imagem .

# Rodar o Container: [sudo] docker run --rm --name go-hello-world-container go-hello-world-imagem

